services:
  syncserver:
    image: python:3.11-slim
    command: >
      sh -c "pip install -q anki && python -m anki.syncserver"
    environment:
      SYNC_USER1: testuser:testpass
      SYNC_PORT: "8080"
      SYNC_HOST: "0.0.0.0"
    ports:
      - "8080:8080"
